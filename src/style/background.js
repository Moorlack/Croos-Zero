import {StyleSheet} from "react-native";

export const background = StyleSheet.create({
    field: {
        width: 385,
        height: 770,
        alignItems: 'center',
        justifyContent: 'center',
    }
});